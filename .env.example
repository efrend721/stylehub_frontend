# Base name for the application (used in routing)
VITE_APP_BASE_NAME=/

# Application version
VITE_APP_VERSION=5.0.0

# Backend API base URL (JWT auth)
# DEV (local)
VITE_APP_API_URL=http://localhost:1234

# PROD (ejemplo): si construyes el frontend para https://vendora.work
# usa el backend público. Nota: estas variables se "hornean" al hacer build.
# VITE_APP_API_URL=https://vendora.work/api

# Environment
# VITE_NODE_ENV=development